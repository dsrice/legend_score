# Development environment variables
# This file is used when running the app in development mode (npm run start)

# API base URL for development
# Leave empty to use relative URLs (same origin)
VITE_API_BASE_URL=http://localhost:1323