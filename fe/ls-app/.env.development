# Development environment variables
# This file is used when running the app in development mode (npm start)

# API base URL for development
# Leave empty to use relative URLs (same origin)
REACT_APP_API_BASE_URL=http://localhost:1323